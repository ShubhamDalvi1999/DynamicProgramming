#include<bits/stdc++.h>
using namespace std;

int memory[MAX];
void intiArr()
{
    for(int i=0;i<MAX;i++)
    {
        memory[i]=-1;
    }
}
int fib(int pos)
{
    if(memory[pos-1]!=-1)
        return memory[pos-1];
    else
    {
        int ans;
        
    }
    
}