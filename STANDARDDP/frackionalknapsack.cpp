#include<bits/stdc++.h>
using namespace std;

struct Item
{
    /* data */
    int value,weight;
    Item(int value, int weight):value(value),weight(weight)
    {}
};

double fractionalkps(int w, Item arr[],int n)
{
    /* data */
    sort(arr,arr+n,cmp)
};

int main()
{
    Item w[]={{60,10},{100,20},{120,30},{140,5}};
    int W=100;
    int n=sizeof(arr)/sizeof(arr[0]);
    int benefit=fractionalkps(W,w,n)
    return 0;
}